language: minimal
script: make
dist: bionic
addons:
  apt:
    packages:
    - emacs-nox
deploy:
  provider: pages:git
  edge: true
  keep_history: true
  deploy_key: ".deploy.key"
  repo: xdp-project/xdp-project.github.io
  target_branch: master
  local_dir: build/
  on:
    branch: master
